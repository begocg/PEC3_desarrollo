{
  "compilerOptions": {
    /* Basic Options */
    "target": "es2015",
    /*target especifica la versión del estándar ECMAScript a la que se dirigirá el código TypeScript después de compilarse*/ 
    "module": "commonjs", 
    /*module especifica el sistema de módulo que utilizará, en este caso, CommonJS*/
    "outDir": "./dist",   
    /*outDir especifica el directorio de salida para los archivos JavaScript compilados*/
    "rootDir": "./src", 
    /*rootDir especifica el directorio raíz de entrada de los archivos TypeScript*/
    "strict": true,
    /*strict habilita o deshabilita la configuración estricta de TypeScript con comprobaciones adicionales*/
    "incremental": true,
    /*incremental habilita o deshabilita la compilación incremental, que significa que TS recordará el estado de la compilación anterior y solo volverá a compìlar los archivos con nuevos cambios*/
    "tsBuildInfoFile": "./buildcache",
    /*tsBuildInfoFile especifica el archivo en el que se almacena información de la compilación para permitir la compilación incremental*/
  }
}
